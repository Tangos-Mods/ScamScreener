# ScamScreener 2.1.3

- 1.21.10 EOL Update!
- Added a direct `Review Case` button to the `Alert Rule Details` screen, above `Close`.
- Warning messages now use `[review]` instead of `[manage]` for the case-review action.
- The public `scamscreener-api` pipeline hooks are now live, so external `PipelineContributor` stages are loaded into the runtime pipeline.
- Simplified internal config and review plumbing with cleanup of unused wrapper layers.
